import { UpbitWs } from './upbit-ws';
export default class UpbitWsTicker extends UpbitWs {
    constructor(codes: string[], cb: {
        open?: any;
        close?: any;
        message?: any;
    }, opts?: {
        isOnlySnapshot: boolean;
        isOnlyRealtime: boolean;
        simpleFormat: boolean;
    });
}
